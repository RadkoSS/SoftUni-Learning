namespace CommandPattern.Common
{
    public static class ExceptionMessages
    {
        public const string InvalidTypeOfCommandMessage = "{0} is invalid! It doesn't exist or it doesn't implement ICommand interface.";
    }
}