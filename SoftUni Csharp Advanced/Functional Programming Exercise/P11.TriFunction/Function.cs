using System;
using System.Linq;
using System.Collections.Generic;

namespace P11.TriFunction
{
    class Function
    {
        static void Main()
        {
            int threshold = int.Parse(Console.ReadLine());

            List<string> names = Console.ReadLine().Split().ToList();

            Func<string, int, bool> checker = name => 
        }
    }
}
